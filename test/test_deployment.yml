
- hosts: all
  connection: local
  vars:
    registryUrl: "{{ lookup('env','registryUrl') }}"
    registryName: "{{ lookup('env','registryName') }}"

  tasks:
    - name: test book-api interface
      shell: "kubectl exec `kubectl get pods |grep book-api|awk '{print $1}'` -- curl -i -H 'Content-Type: application/json' -X POST -d '{\"query\": \"query {book(isbn: \"9780545010221\") {authors}}"}' http://127.0.0.1:8222/graphql\"
    

    